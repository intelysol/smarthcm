# UI Standard

Use typed reusable React components and the design system. Build responsive, accessible experiences with keyboard navigation and loading, empty, and error states. Use server-side controls for large lists.
