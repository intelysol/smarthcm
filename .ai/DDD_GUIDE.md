# DDD Guide

Aggregates protect invariants. Entities have identity; value objects are immutable. Repository interfaces belong to the domain and implementations to infrastructure. Publish domain events for observable facts.
