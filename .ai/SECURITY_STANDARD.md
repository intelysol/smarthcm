# Security Standard

Resolve tenant context server-side. Never trust tenant identifiers from clients. Authorize every mutation, audit sensitive actions, protect secrets in managed configuration, and make asynchronous handlers idempotent.
