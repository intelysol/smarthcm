# Database Standard

Tenant-owned aggregates use UUID identifiers, trusted tenant context, scoped indexes, foreign keys, timestamps, actor attribution, soft deletes, and row versions unless an approved exception is documented.
