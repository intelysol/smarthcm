# API Standard

Version APIs. Validate input with requests, authorize actions, map input to DTOs, invoke use cases, and return resources. Support pagination, filtering, sorting, field selection, and relationship includes where appropriate.
