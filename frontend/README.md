# Flow Frontend

Shared React capability is organized into `pages`, `layouts`, `components`, `widgets`, `hooks`, `store`, `services`, `themes`, and `design-system`. API access is through versioned SDK/service contracts; business rules remain in backend use cases.
